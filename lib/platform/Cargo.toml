[package]
name = "platform"
version = "0.1.0"

[dependencies]
failure = "0.1.1"  
core = { path = "../core/" }

[target.'cfg(target_arch="wasm32")'.dependencies]
js = { path = "../js" }

[target.'cfg(not(target_arch="wasm32"))'.dependencies]
sdl2 = "*"
gl = "*"
ws = "*"